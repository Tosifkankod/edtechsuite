import { NextFunction, Request, Response } from "express";

export default {
    index: async (_: Request, __: Response, ___: NextFunction) => {
        try {

        } catch (error) {

        }
    }
}
